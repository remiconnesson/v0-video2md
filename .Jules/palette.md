# Palette's Journal

## 2025-12-23 - Accessibility Alerts
**Learning:** Dynamic error messages in forms are often missed by screen readers unless explicitly marked with `role="alert"`.
**Action:** Always wrap conditional error messages in a container with `role="alert"` or `aria-live="assertive"`.
